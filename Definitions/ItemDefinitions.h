#ifndef ITEMDEFINITIONS_H
#define ITEMDEFINITIONS_H

namespace ItemDefinitions
{
    enum ItemTypeEnum { eNone = 0, eKey, ePotion, eArmour, eWeapon };

    enum KeyDoorTypeEnum { eIron = 0, eBoss };

    enum PotionTypeEnum { eEmpty = 0, eLow, eMedium, eHigh };
}

#endif // ITEMDEFINITIONS_H
